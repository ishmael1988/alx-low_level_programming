#include "main.h"
/**
 * _abs - computers the adsolute value of an integer
 * @t: the intiger the intiger to be computed
 * Return: the absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
